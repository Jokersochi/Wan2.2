## 2024-04-08 - Added descriptive text to tqdm loading bars in CLI application
**Learning:** Adding a short, actionable description to standard terminal progress bars makes long-running AI generation processes significantly less opaque and friendlier for users.
**Action:** Always add descriptions to iterables passed to `tqdm()` to clarify the specific task loading.
