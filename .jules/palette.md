## 2025-04-12 - Adding Context to CLI Progress Bars
**Learning:** For CLI-based Python tools, adding descriptive text to `tqdm` progress bars significantly improves the user experience during long-running iterations by providing contextual feedback.
**Action:** Always include a descriptive `desc` argument when using `tqdm` loops for long-running processes like sampling steps.
